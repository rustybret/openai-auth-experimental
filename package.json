{
  "name": "@cortexkit/openai-auth",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/opencode && bun run build && cd ../pi && bun run build",
    "dev": "bun scripts/dev.ts",
    "dev:clean": "bun scripts/dev-clean.ts",
    "analyze:cache": "bun scripts/analyze-cache-cliffs.mjs",
    "test": "cd packages/opencode && bun run test && cd ../pi && bun run test",
    "typecheck": "cd packages/opencode && bun run typecheck && cd ../pi && bun run typecheck",
    "types": "bun run typecheck",
    "format": "biome check --write --unsafe .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "pack:opencode:dry": "cd packages/opencode && bun pm pack --dry-run",
    "pack:pi:dry": "cd packages/pi && bun pm pack --dry-run",
    "prepublishOnly": "bun run build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.10",
    "@earendil-works/pi-ai": "0.84.3",
    "@earendil-works/pi-coding-agent": "0.84.3",
    "@earendil-works/pi-tui": "0.84.3",
    "@opencode-ai/plugin": "1.18.23",
    "@tsconfig/bun": "1.0.11",
    "@types/bun": "1.4.0",
    "bun-types": "1.4.0",
    "lefthook": "2.1.10",
    "typescript": "7.0.2"
  }
}
